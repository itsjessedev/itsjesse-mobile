name: itsjesse_mobile
description: "Jesse Eldridge's portfolio - Native mobile app demonstrating Website-to-App conversion with auto-sync capability"
publish_to: 'none'

version: 1.0.8+8

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.2
  dio: ^5.4.0
  path_provider: ^2.1.2
  package_info_plus: ^5.0.1
  open_filex: ^4.4.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
